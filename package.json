{
  "name": "movie-ticket-dapp",
  "version": "1.0.0",
  "description": "Movie ticket booking using ERC20 tokens",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "dotenv": "^16.0.0"
  }
}
